[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -ra
markers =
    unit: Tests unitaires
    integration: Tests d'intégration
    slow: Tests lents (ex: appels LLM réels)
